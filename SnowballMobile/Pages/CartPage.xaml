<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SnowballMobile.PageModels"
             xmlns:models="clr-namespace:SnowballMobile.PageModels"
             x:Class="SnowballMobile.Pages.CartPage"
             x:DataType="vm:CartPageModel"
             Title="Koszyk"
             BackgroundColor="{AppThemeBinding Light={StaticResource LightSecondaryBackground}, Dark={StaticResource DarkSecondaryBackground}}">

    <VerticalStackLayout Padding="24,16" Spacing="18">
        <Label Text="Twój koszyk"
               FontSize="28"
               FontAttributes="Bold"
               HorizontalOptions="Center" />

        <CollectionView ItemsSource="{Binding CartDisplayItems}" Margin="0,10,0,0">
            <CollectionView.EmptyView>
                <Label Text="Koszyk jest pusty"
                       FontSize="18"
                       HorizontalOptions="Center"
                       VerticalOptions="CenterAndExpand"
                       TextColor="{StaticResource Gray300}" />
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:CartPageModel+CartItemViewModel">                    <Frame Padding="12" Margin="0,8" BorderColor="{StaticResource Gray300}" CornerRadius="16" HasShadow="True" BackgroundColor="{AppThemeBinding Light=White, Dark=#232323}">
                        <HorizontalStackLayout Spacing="12" VerticalOptions="Center">
                            <Image Source="{Binding Image}" HeightRequest="60" WidthRequest="60" Aspect="AspectFill" />
                            <VerticalStackLayout Spacing="4" VerticalOptions="CenterAndExpand">
                                <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />
                                <Label Text="{Binding Description}" FontSize="14" TextColor="{AppThemeBinding Light=Black, Dark=White}" />
                                <Label Text="{Binding Price, StringFormat='{}{0:C}'}" FontSize="16" FontAttributes="Bold" />
                            </VerticalStackLayout>
                            <Button Text="Usuń"
                                    Command="{Binding BindingContext.RemoveFromCartCommand, Source={RelativeSource AncestorType={x:Type ContentPage}}}"
                                    CommandParameter="{Binding .}"
                                    BackgroundColor="Red"
                                    TextColor="White"
                                    HorizontalOptions="End"
                                    VerticalOptions="Center" />
                        </HorizontalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Label Text="{Binding CartSummary.TotalPrice, StringFormat='Suma: {0:C}'}"
               FontSize="20"
               FontAttributes="Bold"
               HorizontalOptions="End"
               Margin="0,10,0,0" />

        <HorizontalStackLayout Spacing="12" HorizontalOptions="Center">
            <Button Text="Wyczyść koszyk"
                    Command="{Binding ClearCartCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
                    BackgroundColor="Gray"
                    TextColor="White" />
            <Button Text="Złóż zamówienie"
                    Command="{Binding PlaceOrderCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="White" />
        </HorizontalStackLayout>

        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="{StaticResource Primary}" />
    </VerticalStackLayout>
</ContentPage>